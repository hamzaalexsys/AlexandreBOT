SQL_SERVER=gsalexdb.database.windows.net
SQL_DATABASE=gsalexandre_ma_Junior
# Use an EXISTING SELECT-only account provisioned by the school administrator.
SQL_USER=
SQL_PASSWORD=
GATEWAY_SERVICE_TOKEN=
# Map independently verified identity subjects to existing Parent_ID values.
VERIFIED_PARENT_MAPPINGS={}
# Local presentation only. Keep false with a real SELECT-only SQL principal.
ALLOW_WRITE_CAPABLE_READONLY_TEST=false
HOST=127.0.0.1
PORT=8788
